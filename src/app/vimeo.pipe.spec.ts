/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VimeoPipe } from './vimeo.pipe';

describe('Pipe: Vimeoe', () => {
  it('create an instance', () => {
    let pipe = new VimeoPipe();
    expect(pipe).toBeTruthy();
  });
});
