/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DaysPipe } from './days.pipe';

describe('Pipe: Dayse', () => {
  it('create an instance', () => {
    let pipe = new DaysPipe();
    expect(pipe).toBeTruthy();
  });
});
